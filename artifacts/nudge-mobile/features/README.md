# Features

Feature modules will own user-facing product slices such as tasks, reminders,
memory, and insights. Each module should keep its UI, domain rules, and
feature-specific data coordination close together while depending on shared
services through explicit interfaces.